name: Copilot write access
on:
  push:
permissions:
  contents: write
  pull-requests: write
  metadata: read
  actions: read
jobs:
  enable-copilot-write:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions//checkout@v4
        
    


    
    
